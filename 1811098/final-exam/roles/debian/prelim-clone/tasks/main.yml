---
# tasks file for roles/ubuntu/prelim-clone
- name: Installing git in debian
  apt:
    name: git
    state: present

- name: Cloning github repository in debian
  git:
    repo: "{{ prelim_clone }}"
    dest: /etc/githubrepo
    version: prelim-exam


