---
# tasks file for roles/user-docker
- name: Creating docker group
  group:
    name: docker
    state: present

- name: Add docker group to current user
  user:
    name: eclee-tip
    group: docker
